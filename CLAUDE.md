# 開発ガイドライン

## 言語・コミュニケーション
* ユーザーへの応答、コードの解説、コミットメッセージ等はすべて**日本語**で行ってください。
* 技術用語やコード内の識別子（変数名・関数名など）はそのままで構いません。

## コード変更・コミット時の必須ワークフロー

コードに変更を加える際は、作業の最後に必ず **`README.md` のバージョン表記と `docs/version-history.md` の変更履歴を更新** してください。

### バージョン更新ルール
1. **変更内容の分析**:
   - 破壊的変更がある場合 $\rightarrow$ メジャーバージョン（A）をインクリメント
   - 新機能の追加・機能拡張の場合 $\rightarrow$ マイナーバージョン（B）をインクリメント
   - バグ修正・マイナーな調整の場合 $\rightarrow$ パッチ/ビルドバージョン（C）をインクリメント
2. **`README.md` 上部（最新バージョン表記）の更新**:
   - ヘッダー部分の `バージョン: X.Y.Z` を最新の番号に更新する。
   - 上部にある区分テーブル（メジャー/マイナー/ビルドの説明）の内容を、**今回の最新状態**に合わせて書き換える。
3. **`docs/version-history.md`（バージョン履歴）へのアーカイブ**:
   - 更新前の旧バージョン情報（バージョン番号・各区分の説明・日付等）は消去せず、**`docs/version-history.md` の変更履歴テーブル先頭へ新しい行を追記**する（過去の行は消さない）。
   - `README.md` 下部の「バージョン履歴」セクションは `docs/version-history.md` への参照リンクのみを置く。

## 実装前の事前確認・問題解消

機能開発や修正を進める際、以下のような懸念事項がある場合は**コードを実装・変更する前に必ずユーザー（人間）に確認・相談**を行ってください。

* 仕様や要求事項に不明点や曖昧な点がある場合
* 既存コードや他の機能との間で矛盾が発生する場合
* 想定通りに動作しない可能性や、予期せぬ副作用・トレードオフがある場合